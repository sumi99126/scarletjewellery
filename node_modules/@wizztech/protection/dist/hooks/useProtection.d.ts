import { ProtectionContextValue } from '../types';
/**
 * Custom React hook exposing WizzTech protection state, status metadata, and retry actions
 */
export declare function useProtection(): ProtectionContextValue;
