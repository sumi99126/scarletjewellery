import { AxiosInstance } from 'axios';
import { ProtectionStatusResponse } from '../types';
/**
 * Constructs full Netlify function validation URL from base platformUrl
 */
export declare function getValidateEndpointUrl(platformUrl: string): string;
/**
 * Creates configured Axios instance targeting Netlify validation function
 */
export declare function createApiClient(platformUrl: string): AxiosInstance;
/**
 * Sends website validation request to WizzTech Platform /.netlify/functions/validate
 */
export declare function validateWebsiteDomain(platformUrl: string, incrementViewOverride?: boolean): Promise<ProtectionStatusResponse>;
