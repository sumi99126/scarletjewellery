import { default as React } from 'react';
import { ProtectionState } from '../types';
interface SecurityIconProps {
    state?: ProtectionState;
}
export declare const SecurityIcon: React.FC<SecurityIconProps>;
export {};
