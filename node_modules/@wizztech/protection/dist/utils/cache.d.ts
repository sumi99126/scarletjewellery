import { ProtectionStatusResponse } from '../types';
/**
 * Saves validated protection status into sessionStorage with TTL
 */
export declare function setCachedSession(domain: string, data: ProtectionStatusResponse): void;
/**
 * Retrieves valid session cache if present and non-expired
 */
export declare function getCachedSession(domain: string, ttlMinutes?: number): ProtectionStatusResponse | null;
/**
 * Clears stored protection session cache from sessionStorage
 */
export declare function clearCachedSession(domain: string): void;
