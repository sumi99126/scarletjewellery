import { default as React } from 'react';
import { ProtectionState, ProtectionStatusResponse } from '../types';
interface ExpiredBlockedScreenProps {
    state: ProtectionState;
    customLogo?: React.ReactNode;
    statusDetails?: ProtectionStatusResponse | null;
}
export declare const ExpiredBlockedScreen: React.FC<ExpiredBlockedScreenProps>;
export {};
